# MySQL 连接串示例
# 如果密码里有 @、:、/ 等特殊字符，请先做 URL 编码，例如 @ -> %40
DATABASE_URL="mysql://root:password@127.0.0.1:3306/kaoyan_notes"

# NextAuth 配置
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-secret"
